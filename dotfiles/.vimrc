syntax on
colorscheme delek
set backup
set ignorecase
set updatetime=3000
autocmd CursorHoldI * silent update
inoremap jj <ESC> 
set background=dark

" TODO whot to do backup so that it creates the bu files with format .filename< se hassu squigly line >
